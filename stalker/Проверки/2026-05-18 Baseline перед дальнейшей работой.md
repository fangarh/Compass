# 2026-05-18 Baseline перед дальнейшей работой

## Цель

Проверить, что перед новой разработкой есть чистая понятная точка отсчета:

- Obsidian-синхронизация создана;
- `task.md` не висит пустым untracked-файлом;
- Git работает без `dubious ownership`;
- сборочная проверка либо выполнена, либо явно заблокирована.

## Что сделано

- Настроен `git config --global --add safe.directory D:/Projects/FL/Compass`.
- `task.md` превращен из пустого файла в короткий task inbox.
- Obsidian-папка `stalker/` наполнена базовыми заметками.
- `stalker/.obsidian/` добавлен в `.gitignore` как локальное состояние редактора; Markdown-заметки остаются tracked.

## Git

После настройки `safe.directory` обычная команда работает:

```powershell
git status --short
```

На момент проверки Git показывает новые baseline-файлы:

```text
?? stalker/
?? task.md
```

## Сборочная проверка

Сборка в текущем shell-окружении заблокирована: необходимые Android-инструменты не найдены.

Проверенные команды:

```powershell
gradle --version
where.exe java
where.exe adb
where.exe gradle
```

Результат:

```text
gradle: command not found
java: not found
adb: not found
gradle: not found
```

Также не найдены ожидаемые пути:

```text
C:\Users\Fangarh\.gradle\wrapper\dists
C:\Program Files\Android\Android Studio\jbr\bin\java.exe
C:\Program Files\Android\openjdk\jdk-21.0.8\bin\java.exe
%LOCALAPPDATA%\Android\Sdk
```

## Вывод

Документационный baseline подготовлен. Перед задачами, которые требуют сборки или emulator QA, нужно восстановить Android toolchain в окружении:

- Java/JDK;
- Gradle или Gradle wrapper;
- Android SDK;
- adb.
